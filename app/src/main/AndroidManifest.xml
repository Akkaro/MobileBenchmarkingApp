<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileBenchmarking"
        tools:targetApi="31">

        <activity android:name=".TestsActivity" />
        <activity android:name=".TestHistoryActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".CPUActivity" />
        <activity android:name=".MemoryGpuActivity" />
        <activity android:name=".HardwareTestsActivity" />
        <activity android:name=".OverallTestActivity" />
        <activity android:name=".history.TestHistoryChartActivity" />
        <activity android:name=".history.TestHistoryTextActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>